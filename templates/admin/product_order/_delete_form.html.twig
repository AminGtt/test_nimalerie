<form method="post" action="{{ path('product_order_delete', {'id': product_order.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_order.id) }}">
    <button class="button delete_btn">Supprimer</button>
</form>
