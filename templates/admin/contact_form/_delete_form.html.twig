<form method="post" action="{{ path('admin_contact_form_delete', {'id': contact_form.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_form.id) }}">
    <button class="button delete_btn">Supprimer</button>
</form>
