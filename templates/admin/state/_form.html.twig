{{ form_start(form) }}
    <div class="alert-danger rounded">
        {{ form_errors(form.name) }}
    </div>
    {{ form_label(form.name) }}
    {{ form_widget(form.name) }}
    <button class="button edit_btn my-5">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
