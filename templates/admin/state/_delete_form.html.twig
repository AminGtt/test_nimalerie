<form method="post" action="{{ path('state_delete', {'id': state.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer ceci?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ state.id) }}">
    <button class="button delete_btn">Supprimer</button>
</form>
